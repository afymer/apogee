[package]
name = "apogee_app"
version = "0.1.0"
edition.workspace = true

[dependencies]
gpui.workspace = true
gpui_platform.workspace = true
wgpu.workspace = true
apogee_planetarium = { path = "../apogee_planetarium" }

[dev-dependencies]
gpui = { workspace = true, features = ["test-support"] }

[lints]
workspace = true
