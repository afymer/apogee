[package]
name = "apogee_app"
version = "0.1.0"
edition.workspace = true

[dependencies]
gpui.workspace = true

[dev-dependencies]
gpui = { workspace = true, features = ["test-support"] }

[lints]
workspace = true
