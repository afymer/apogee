[package]
name = "theme"
version = "0.1.0"
edition.workspace = true

[lib]
name = "theme"
path = "src/theme.rs"

[dependencies]
gpui.workspace = true

[dev-dependencies]
gpui = { workspace = true, features = ["test-support"] }

[lints]
workspace = true
