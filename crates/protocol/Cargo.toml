[package]
name = "protocol"
version = "0.1.0"
edition.workspace = true

[lib]
name = "protocol"
path = "src/protocol.rs"

[dependencies]
serde.workspace = true
tracing.workspace = true

[lints]
workspace = true
